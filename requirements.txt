# Direct dependencies, pinned. Installed into the bundled runtime at .\python
# by scripts\setup_env.ps1 (transitive dependencies are resolved by pip).
PySide6==6.11.1
numpy==2.5.0
scipy==1.18.0
matplotlib==3.11.0
pyinstaller==6.21.0
